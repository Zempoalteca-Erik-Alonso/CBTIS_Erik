#include <iostream>
using namespace std;
int main() {
    float base, altura;
    cout << "Ingresa los valores de la base y de la altura del rectangulo: ";
    cin >> base >> altura;
    cout << "El area es: " << base * altura << endl;
    return 0;
}
