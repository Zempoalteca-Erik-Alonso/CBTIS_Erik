#include <iostream>
using namespace std;
int main() {
    float base, altura;
    cout << "escribe los valores de la base y de la altura del triangulo: ";
    cin >> base >> altura;
    cout << "El area es: " << (base * altura) / 2 << endl;
    return 0;
}
,